# Configuração do pnpm para Vercel
# Permite scripts de build de dependências nativas
ignore-build-scripts=false

# Aprova builds automaticamente para estes pacotes
only-built-dependencies[]
only-built-dependencies[]=@tailwindcss/oxide
only-built-dependencies[]=esbuild
